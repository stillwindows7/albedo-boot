<?xml version="1.0" encoding="utf-8"?>
<template>
    <name>viewComponentTs</name>
    <filePath>src/main/webapp/app/theme/pages/${lastPackageName}/${moduleName}/${subModuleName}/${className}/service</filePath>
    <fileName>${className}.model.ts</fileName>
    <content><![CDATA[
import {Tree} from "../../../../../../shared/base/model/tree.model"

export class ${ClassName} extends Tree {

    <#list table.columnList as column>
		<#if !column.pk && column.isNotBaseField>
	/** ${column.simpleJavaField} ${column.comments} */
	        <#if column.showType == "userselect" || column.showType == "orgselect" || column.showType == "areaselect">
	public ${column.simpleJavaField}Id?: string
	        <#else>
	public ${column.simpleJavaField}?: ${column.simpleTsType}
			</#if>
		</#if>
	</#list>

}

]]>
    </content>
</template>
